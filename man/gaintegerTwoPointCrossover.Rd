% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gaintegerTwoPointCrossover.R
\name{gaintegerTwoPointCrossover}
\alias{gaintegerTwoPointCrossover}
\title{Integer-valued two-point crossover operator}
\usage{
gaintegerTwoPointCrossover(object, parents, ...)
}
\arguments{
\item{object}{A \code{GA} object from the \code{GA} package; uses \code{object@population}
as the matrix of chromosomes.}

\item{parents}{Integer vector of length 2 giving the row indices of the two
parents in \code{object@population}.}

\item{...}{Ignored; included for compatibility with the \code{GA} package interface.}
}
\value{
A list with elements \code{children} (2 x nGenes integer matrix) and
\code{fitness} (numeric vector of length 2 filled with \code{NA}).
}
\description{
Perform a two-point crossover on two parent chromosomes. Two crossover points
are sampled uniformly, ordered as \code{p1 < p2}, and the outer segments
(positions \verb{1..p1} and \code{p2..end}) are swapped between parents. Intended for
use as the \code{crossover} function in \code{GA::ga()}.
}
\details{
Returns two children with the same integer encoding and length as
the parents. Fitness values are not computed here and are returned as
\code{NA_real_} placeholders.
}
\seealso{
GA::ga
}
