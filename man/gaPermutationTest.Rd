% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gaPermutationTest.R
\name{gaPermutationTest}
\alias{gaPermutationTest}
\title{Y-scrambling permutation test for GA-based variable selection}
\usage{
gaPermutationTest(
  object,
  x,
  nPermutations = 500,
  seed = NULL,
  validateSettings = FALSE,
  verbose = FALSE,
  workers = 1,
  ...
)
}
\arguments{
\item{object}{A \code{gaQSAR} or \code{gaQSAR_dcv} object representing the true (unpermuted)
result.}

\item{x}{A data.frame or matrix of predictors (same as used in the original analysis).}

\item{nPermutations}{Integer; number of permutations to perform. Default is 500.}

\item{seed}{Integer; random seed for reproducibility of permutations. If \code{NULL},
permutations are non-deterministic.}

\item{validateSettings}{Logical; if \code{TRUE}, re-runs the true (unpermuted) analysis
using the stored settings to verify they reproduce the original result. If the
re-run metric differs from the stored metric by more than 0.001, a warning is issued.
Default is \code{FALSE}.}

\item{verbose}{Logical; if \code{TRUE}, print progress information for each permutation.
Default is \code{FALSE}.}

\item{workers}{Integer; number of parallel workers to use for permutations. Use \code{1}
to run sequentially. When \code{workers > 1}, the function uses a temporary
\code{future::multisession} plan and \code{future.apply::future_lapply()} with
\code{future.seed = TRUE}, restoring the previous plan on exit.}

\item{...}{Currently unused.}
}
\value{
A list of class "gaQSAR_permTest" containing:
\itemize{
\item trueMetric: Numeric; the true performance metric (Q2 or outer Q2).
\item permutedMetrics: Numeric vector of length \code{nPermutations} with performance
metrics from permuted runs.
\item pValue: Numeric; empirical p-value with plus-one correction.
\item nPermutations: Integer; number of permutations performed.
\item objectType: Character; either "gaQSAR" or "gaQSAR_dcv".
\item seed: Integer or NULL; random seed used.
}
}
\description{
Performs a Y-scrambling permutation test for objects produced by gaQSAR
or gaQSAR_dcv. In each permutation, the response vector is randomly
permuted while the descriptor matrix is kept unchanged. The GA variable
selection procedure is then repeated using the same settings as in the
original analysis.
}
\details{
The permutation test works by:
\enumerate{
\item Extracting the true performance metric (Q2 for gaQSAR, outer Q2 for gaQSAR_dcv).
\item For each permutation: scrambling y, re-running the analysis, and recording the
permuted performance metric.
\item The empirical p-value is computed with a plus-one correction:
\code{(1 + nGreaterEqual) / (1 + nValid)}, where \code{nGreaterEqual} is the number of
valid permutations with a performance metric greater than or equal to the
observed metric, and \code{nValid} is the number of valid permutation runs.
}
}
\examples{
# Simple example with gaQSAR object
set.seed(42)
n <- 50
p <- 20
x <- matrix(rnorm(n * p), nrow = n)
colnames(x) <- paste0("X", seq_len(p))
y <- 2 * x[, 2] - 1.5 * x[, 5] + rnorm(n, sd = 0.5)

# NOTE: Settings below are example-only (fast runtime, not optimal).
# For real QSAR work, use larger population sizes, more generations, and multiple seeds.
# Good starting point are the default GA settings, or run an experimental design to
# tune GA settings for your dataset.

fit <- gaVariableSelection(
  x = x, 
  y = y,
  numberOfVariables = 3,
  popSize = 10,
  maxIter = 10,
  seeds = 1, 
  interval = 5,
  verbose = TRUE
)

# For a meaningful permutation test, use a larger number of permutations (e.g., 100 or more).
permTest <- gaPermutationTest(
  object = fit,
  x = x,
  nPermutations = 2,  # small number for example; use 100+ for real tests
  verbose = TRUE
)
print(permTest)

}
\seealso{
\code{\link[=gaVariableSelection]{gaVariableSelection()}}, \code{\link[=gaDoubleCrossValidation]{gaDoubleCrossValidation()}}
}
