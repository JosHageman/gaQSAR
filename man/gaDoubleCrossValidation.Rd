% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gaDoubleCrossValidation.R
\name{gaDoubleCrossValidation}
\alias{gaDoubleCrossValidation}
\title{Nested (double) cross-validation for GA-based variable selection}
\usage{
gaDoubleCrossValidation(
  x,
  y,
  outerMethod = "kfold",
  outerK = 5,
  seed = NULL,
  residualThreshold = 2.5,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{x}{A data.frame or matrix of predictors (rows = observations,
columns = candidate variables).}

\item{y}{Numeric response vector aligned with rows of \code{x}.}

\item{outerMethod}{Character; outer CV method: "loo" for leave-one-out
or "kfold" for k-fold cross-validation (default).}

\item{outerK}{Integer; number of folds for outer k-fold CV (only used if
\code{outerMethod = "kfold"}). Defaults to 5.}

\item{seed}{Integer; random seed for reproducible fold splits (especially relevant
for k-fold CV). If \code{NULL}, fold splits are non-deterministic.}

\item{residualThreshold}{Numeric; threshold for standardized residuals in Williams plot diagnostics.}

\item{verbose}{Logical; if \code{TRUE}, print brief progress information per outer fold.
Defaults to \code{FALSE} (silent operation).}

\item{...}{Additional arguments forwarded to \code{gaVariableSelection()}, such as
\code{numberOfVariables}, \code{popSize}, \code{maxIter}, \code{pMutation}, \code{pCrossover},
and other GA settings.}
}
\value{
An object of class "gaQSAR_dcv" (list) containing:
\itemize{
\item call: The matched call.
\item outer: List with elements \code{method}, \code{k}, \code{seed} (the outer fold seed for reproducibility),
and \code{folds} (list of outer fold indices).
\item outerPredictions: Numeric vector of length n (full dataset) with out-of-fold predictions.
\item outerResiduals: Numeric vector of length n with outer residuals.
\item yObserved: Original observed response vector (aligned with rows of \code{x}).
\item outerQ2: Numeric; Q2 computed from outer predictions (NA if < 2 valid predictions).
\item foldModels: List of gaQSAR objects (one per outer fold; NULL for failed folds).
\item foldSummaries: Data.frame with one row per outer fold, including columns:
nPredictors, selectedPredictors, innerQ2 (LOOCV fitness), trainingR2,
trainingR2Adj, maxVif, bestSeed (the GA seed that produced the best model for that fold),
status ("ok" or "failed"), errorMessage.
\item foldDiagnostics: List with Williams plot diagnostic data per fold (NULL for failed folds);
each element includes williamsData, hStar, residualThreshold.
\item adThresholds: List describing applicability domain thresholds used:
leverageCutoffMethod, leverageCutoff (NA if varies), residualCutoff, notes.
\item selectionFrequency: Named numeric vector of predictor frequencies (0 to 1)
computed over successful folds only.
\item selectionFrequencyAllFolds: Named numeric vector of predictor frequencies
(0 to 1) computed over all folds, including failed folds.
\item signStability: Named numeric vector of positive sign frequency for selected predictors.
\item modelSizeDistribution: Named integer vector of model size frequencies.
\item perObjectOutlierFrequency: Numeric vector of length n with outlier frequencies.
\item perObjectHighLeverageFrequency: Numeric vector of length n with high-leverage frequencies.
}
}
\description{
Performs nested cross-validation combining an outer CV loop (for final model
assessment) with an inner CV loop (used by the GA for fitness evaluation during
variable selection). The inner loop always uses LOOCV via \code{singleCV} as the
GA fitness. The outer loop may be leave-one-out (default) or k-fold, providing
an unbiased estimate of outer-loop predictive performance and stability analysis
of selected predictors.
}
\details{
For each outer fold:
\enumerate{
\item Create outerTrain (all data except outerTest) and outerTest (hold-out fold).
\item Run gaVariableSelection() on outerTrain using inner CV for fitness evaluation.
\item Fit the selected lm model on outerTrain using selected predictors.
\item Predict on outerTest and store predictions and residuals.
\item Compute and store diagnostic measures (Williams plot data, VIF, etc.).
}

Two types of seeds are employed:
\itemize{
\item The \code{seed} parameter (outer fold seed) controls reproducible creation of outer CV folds.
\item The \code{seeds} parameter (passed via \code{...} to gaVariableSelection) enables multiple GA runs
for robustness within each fold. The best seed is tracked in \code{foldSummaries$bestSeed}.
}

Across all outer folds, summaries are computed:
\itemize{
\item selectionFrequency: How often each predictor is selected across successful
outer folds.
\item selectionFrequencyAllFolds: Optional frequency over all outer folds,
including failed folds.
\item signStability: For each selected predictor, the proportion of times its
coefficient is positive (vs negative) across folds.
\item modelSizeDistribution: Distribution of numbers of selected predictors.
\item perObject outlier/high-leverage frequencies based on Williams plot thresholds.
}
}
\examples{
\dontrun{
  set.seed(42)
  n <- 50
  p <- 20
  x <- matrix(rnorm(n * p), nrow = n)
  colnames(x) <- paste0("X", seq_len(p))
  y <- 2 * x[, 2] - 1.5 * x[, 5] + rnorm(n, sd = 0.5)

  # Nested CV with LOO outer and inner LOOCV fitness (slow, but thorough)
  dcvFit <- gaDoubleCrossValidation(
    x = x, y = y,
    outerMethod = "loo",
    numberOfVariables = 3,
    popSize = 30,
    maxIter = 100,
    seed = 1
  )

  print(dcvFit)
  summary(dcvFit)
  plot(dcvFit)
  plot(dcvFit, type = "selectionFrequency")
  plot(dcvFit, type = "williamsFrequency")
}

}
\seealso{
\code{\link[=gaVariableSelection]{gaVariableSelection()}}, \code{\link[=singleCV]{singleCV()}}, \code{\link[=Q2]{Q2()}}, \code{\link[=createWilliamsPlot]{createWilliamsPlot()}}
}
