% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/splitUp.R
\name{splitUp}
\alias{splitUp}
\title{Split data into training and test sets}
\usage{
splitUp(
  data,
  method = c("KS", "random"),
  trainPercentage = 0.8,
  pc = 0.95,
  verbose = FALSE
)
}
\arguments{
\item{data}{Data frame or matrix with observations in rows (predictors and/or
response may be present; only row indices are returned).}

\item{method}{Character; one of \code{"KS"} (Kennard-Stone) or \code{"random"}.}

\item{trainPercentage}{Numeric in (0, 1); fraction of observations assigned to
the training set.}

\item{pc}{Numeric; proportion of variance to retain in the PCA step used by
\code{prospectr::kenStone()} when \code{method = "KS"}.}

\item{verbose}{Logical; if \code{TRUE}, prints the chosen training and test IDs (row names).}
}
\value{
A list with integer vectors \code{model} (training indices) and \code{test} (test indices).
}
\description{
Split observations into training and test sets using either a Kennard-Stone
split ("KS") based on sample representativeness or a simple random split.
The function returns row indices for both sets.
}
\details{
For \code{method = "KS"}, the function requires the \code{prospectr} package
and delegates to \code{prospectr::kenStone()} with \code{k = round(trainPercentage * n)}
on a scaled version of \code{data}. For \code{method = "random"}, rows are permuted and
the first \code{k} indices are assigned to training.
}
\examples{
set.seed(123)
toy <- data.frame(x = rnorm(40), y = rnorm(40))
split <- splitUp(toy, method = "random", trainPercentage = 0.75)
str(split)

}
