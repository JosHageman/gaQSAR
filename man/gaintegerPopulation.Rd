% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gaintegerPopulation.R
\name{gaintegerPopulation}
\alias{gaintegerPopulation}
\title{Integer-valued GA population initializer}
\usage{
gaintegerPopulation(object, ...)
}
\arguments{
\item{object}{A \code{GA} object from the \code{GA} package; reads \code{object@upper}
(gene-wise upper bounds) and \code{object@popSize} (number of individuals).}

\item{...}{Ignored; present for compatibility with the \code{GA} package interface.}
}
\value{
A numeric matrix with dimensions \verb{popSize x length(upper)} containing
the initial population.
}
\description{
Create an initial population for integer-encoded chromosomes. For each gene
position \code{j}, values are drawn independently and uniformly from the range
\code{1:object@upper[j]}. Intended for use as the \code{population} function in
\code{GA::ga()}.
}
\details{
The returned matrix has one row per individual and one column per
gene. Sampling uses \code{sample.int()} with replacement.
}
\seealso{
GA::ga
}
