% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/singleCV.R
\name{singleCV}
\alias{singleCV}
\title{LOOCV Q2 fitness function for small datasets}
\usage{
singleCV(predictors, x, y, vifThreshold = 5)
}
\arguments{
\item{predictors}{Integer vector of 1-based predictor indices referring to
columns of \code{x}.}

\item{x}{Matrix or data.frame of predictors (rows = observations,
columns = candidate variables).}

\item{y}{Numeric response vector aligned with rows of \code{x}.}

\item{vifThreshold}{Numeric; maximum allowed VIF (default 5).}
}
\value{
A numeric scalar: $Q^2$ if valid; 0 if the VIF constraint fails; or
-100 if duplicate predictors are present.
}
\description{
Compute a leave-one-out cross-validated $Q^2$ for a candidate set of
predictors. Designed for use as a fitness function inside a genetic
algorithm. Duplicate predictors are penalized with a large negative value,
and candidate sets with variance inflation factor (VIF) above a threshold are
rejected (fitness 0).
}
\details{
An intercept is added internally. VIF values are computed by
regressing each selected predictor on the remaining selected predictors; if
any VIF exceeds \code{vifThreshold}, the fitness is 0. LOOCV is performed via
explicit refits for each left-out observation.
}
